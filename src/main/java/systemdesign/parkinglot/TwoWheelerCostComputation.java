package systemdesign.parkinglot;

public class TwoWheelerCostComputation extends CostComputation {
    public TwoWheelerCostComputation(IPricingStrategy pricingStrategy) {
        super(pricingStrategy);
    }
}
